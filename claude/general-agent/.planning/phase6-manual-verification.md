# Phase 6 手工验证清单

**验证日期：** 2026-03-06
**验证人：** _______
**环境：** macOS / Python 3.14.3

---

## 前提条件

### 1. 环境准备
```bash
# 确认当前在 phase6 worktree
pwd
# 应该显示：.../general-agent/.worktrees/phase6-tui/python/general-agent

# 激活虚拟环境（如果有）
source .venv/bin/activate

# 确认依赖已安装
pip list | grep -E "(textual|typer)"
```

### 2. 配置环境变量
```bash
# 复制 .env.example 到 .env（如果还没有）
cp .env.example .env

# 使用 Mock 模式（推荐用于测试）
echo "USE_OLLAMA=false" >> .env

# 或使用真实 Ollama（需要先启动 Ollama）
# echo "USE_OLLAMA=true" >> .env
# ollama serve  # 在另一个终端运行
```

---

## 验证清单

### ✅ 1. 命令行模式（快速查询）

#### 1.1 基本查询
```bash
agent "你好，请介绍一下你自己"
```
**预期结果：**
- [ ] 命令正常执行
- [ ] 返回 Agent 响应
- [ ] 自动退出（不进入 TUI）

#### 1.2 帮助信息
```bash
agent --help
```
**预期结果：**
- [ ] 显示完整帮助文档
- [ ] 包含所有命令选项（--tui, --session, --verbose）

#### 1.3 版本信息
```bash
agent --version
```
**预期结果：**
- [ ] 显示版本号

#### 1.4 空查询处理
```bash
agent ""
```
**预期结果：**
- [ ] 显示错误提示或帮助信息

#### 1.5 Ollama 未启动错误（仅当 USE_OLLAMA=true）
```bash
# 确保 Ollama 未运行
# 设置环境变量
USE_OLLAMA=true agent "测试"
```
**预期结果：**
- [ ] 显示友好错误消息
- [ ] 提示如何启动 Ollama 或切换到 Mock 模式
- [ ] 不崩溃，正常退出

---

### ✅ 2. TUI 模式（交互式界面）

#### 2.1 启动 TUI
```bash
agent --tui
```
**预期结果：**
- [ ] TUI 正常启动
- [ ] 显示标题栏："General Agent - Session: [session-id]"
- [ ] 显示消息区域（空白）
- [ ] 显示输入框："> 输入消息..."
- [ ] 显示底部快捷键提示

#### 2.2 发送消息
**操作：** 在输入框输入 "你好" 并按 Enter

**预期结果：**
- [ ] 显示用户消息："🧑 User: 你好"
- [ ] 显示 "正在思考..." 动画
- [ ] "正在思考..." 消失后显示 Agent 响应
- [ ] 消息格式正确（用户/Agent 区分）
- [ ] 自动滚动到最新消息

#### 2.3 长消息换行
**操作：** 输入一条很长的消息（100+ 字符）

**预期结果：**
- [ ] 消息自动换行
- [ ] 不出现横向滚动条
- [ ] 可读性良好

#### 2.4 多轮对话
**操作：** 连续发送 3-5 条消息

**预期结果：**
- [ ] 所有消息正确显示
- [ ] 消息顺序正确
- [ ] 历史消息可滚动查看
- [ ] 界面不卡顿

---

### ✅ 3. 会话管理

#### 3.1 创建新会话（Ctrl+N）
**操作：** 在 TUI 中按 Ctrl+N

**预期结果：**
- [ ] 消息区域清空
- [ ] 标题栏显示新的 session ID
- [ ] 可以正常发送消息
- [ ] 旧会话数据保留（可通过会话列表验证）

#### 3.2 会话列表（Ctrl+L）
**操作：** 按 Ctrl+L

**预期结果：**
- [ ] 弹出会话列表对话框
- [ ] 显示所有会话（至少有 1 个）
- [ ] 每个会话显示：ID、标题、相对时间
- [ ] 当前会话有标记（● 或高亮）
- [ ] 显示 "[N] 新建会话" 选项

#### 3.3 切换会话
**操作：**
1. 按 Ctrl+L 打开会话列表
2. 用方向键选择另一个会话
3. 按 Enter 确认

**预期结果：**
- [ ] 对话框关闭
- [ ] 标题栏显示所选会话 ID
- [ ] 消息区域显示该会话的历史消息
- [ ] 可以继续对话

#### 3.4 会话列表新建（N 键）
**操作：**
1. 按 Ctrl+L 打开会话列表
2. 按 N 键

**预期结果：**
- [ ] 关闭会话列表
- [ ] 创建新会话（同 Ctrl+N）

#### 3.5 取消会话选择（Esc）
**操作：**
1. 按 Ctrl+L 打开会话列表
2. 按 Esc

**预期结果：**
- [ ] 对话框关闭
- [ ] 保持当前会话不变

#### 3.6 加载指定会话
```bash
# 先获取一个会话 ID
# 可以从数据库查看：sqlite3 data/general_agent.db "SELECT id FROM sessions LIMIT 1;"
# 或从 TUI 会话列表中复制

agent --tui --session=session-abc123
```
**预期结果：**
- [ ] TUI 启动并加载指定会话
- [ ] 显示该会话的历史消息
- [ ] 可以继续对话

---

### ✅ 4. 快捷键

#### 4.1 清屏（Ctrl+K）
**操作：** 发送几条消息后按 Ctrl+K

**预期结果：**
- [ ] 消息区域清空
- [ ] 不影响会话数据（消息仍在数据库）
- [ ] 可以继续发送消息

#### 4.2 退出（Ctrl+Q）
**操作：** 按 Ctrl+Q

**预期结果：**
- [ ] TUI 正常退出
- [ ] 返回命令行
- [ ] 无错误消息

---

### ✅ 5. 会话共享（CLI ↔ Web）

#### 5.1 CLI → Web
**操作：**
1. 在 TUI 中创建会话并发送消息
2. 记录会话 ID（从标题栏）
3. 启动 Web 服务器：
   ```bash
   # 在主项目目录（非 worktree）
   cd /Users/shichang/Workspace/program/python/general-agent
   uvicorn src.main:app --reload
   ```
4. 打开浏览器访问 http://localhost:8000
5. 查找刚才的会话

**预期结果：**
- [ ] Web 界面显示该会话
- [ ] 消息内容一致
- [ ] 时间戳正确

#### 5.2 Web → CLI
**操作：**
1. 在 Web 界面创建新会话并发送消息
2. 记录会话 ID
3. 在 CLI 中加载该会话：
   ```bash
   agent --tui --session=[web创建的session-id]
   ```

**预期结果：**
- [ ] TUI 加载该会话
- [ ] 显示 Web 中发送的消息
- [ ] 可以继续对话

#### 5.3 并发访问
**操作：**
1. 同时打开 TUI 和 Web
2. 在 TUI 中发送消息
3. 在 Web 中刷新页面

**预期结果：**
- [ ] Web 中看到新消息（刷新后）
- [ ] 无数据库锁定错误
- [ ] 两边都能正常工作

---

### ✅ 6. 错误处理

#### 6.1 网络超时（仅 USE_OLLAMA=true）
**操作：**
1. 设置短超时：`OLLAMA_TIMEOUT=1`
2. 发送复杂查询

**预期结果：**
- [ ] 显示超时错误
- [ ] 不崩溃
- [ ] 可以继续使用

#### 6.2 无效会话 ID
```bash
agent --tui --session=invalid-session-id
```
**预期结果：**
- [ ] 自动创建新会话（或显示错误）
- [ ] 不崩溃

#### 6.3 数据目录不存在
```bash
# 临时删除 data 目录
mv data data.bak

# 运行 TUI
agent --tui
```
**预期结果：**
- [ ] 自动创建 data 目录
- [ ] 启动检查显示创建信息（如果 --verbose）
- [ ] 正常运行

```bash
# 恢复
mv data.bak data
```

#### 6.4 .env 文件缺失
```bash
# 临时删除 .env
mv .env .env.bak

# 运行 TUI（verbose 模式查看警告）
agent --tui --verbose
```
**预期结果：**
- [ ] 显示警告：".env 文件不存在，使用默认配置"
- [ ] 使用默认配置继续运行
- [ ] 不崩溃

```bash
# 恢复
mv .env.bak .env
```

---

### ✅ 7. 详细日志模式

```bash
agent --tui --verbose
```
**预期结果：**
- [ ] 显示启动检查详细信息
- [ ] 显示数据库操作日志
- [ ] 显示 LLM 调用日志
- [ ] 帮助调试问题

---

## 验证结果总结

### 通过的测试
- [ ] 1. 命令行模式 (5/5)
- [ ] 2. TUI 模式 (4/4)
- [ ] 3. 会话管理 (6/6)
- [ ] 4. 快捷键 (2/2)
- [ ] 5. 会话共享 (3/3)
- [ ] 6. 错误处理 (4/4)
- [ ] 7. 详细日志 (1/1)

**总计：** _____ / 25

### 发现的问题
1.
2.
3.

### 备注

---

**验证完成时间：** _______
**签名：** _______
